package OOPS.Interface;

interface Car {
    // Whatever you declare inside an interface this static final by nature So we no need to specify the static and final interfaces
    void acc();
    void brake();
    void seats();
}
